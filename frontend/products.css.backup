/* ============================================
   ROYAL STEEL EGYPT - PRODUCTS PAGE
   Dedicated Styles for products.html
   ============================================ */

/* ================= BASE / VARIABLES ================= */
:root {
  --bg-primary: #070a0f;
  --bg-secondary: #0d1117;
  --bg-tertiary: #111820;
  --bg-card: rgba(17, 24, 36, 0.7);
  --bg-glass: rgba(13, 17, 23, 0.85);
  --text-primary: #f0f4f8;
  --text-secondary: #9fb0c3;
  --text-muted: #6b7a8f;
  --accent-gold: #d4a843;
  --accent-gold-light: #e8c97a;
  --accent-gold-dark: #b8922e;
  --accent-steel: #7ba3c9;
  --accent-steel-light: #a8c4e0;
  --gradient-gold: linear-gradient(135deg, #d4a843 0%, #e8c97a 50%, #d4a843 100%);
  --gradient-steel: linear-gradient(135deg, #7ba3c9 0%, #a8c4e0 50%, #7ba3c9 100%);
  --gradient-card: linear-gradient(180deg, rgba(17,24,36,0.9) 0%, rgba(17,24,36,0.6) 100%);
  --shadow-sm: 0 2px 8px rgba(0,0,0,0.3);
  --shadow-md: 0 8px 24px rgba(0,0,0,0.4);
  --shadow-lg: 0 16px 48px rgba(0,0,0,0.5);
  --shadow-gold: 0 8px 32px rgba(212,168,67,0.2);
  --shadow-gold-lg: 0 12px 40px rgba(212,168,67,0.3);
  --glow-gold: 0 0 20px rgba(212,168,67,0.3);
  --border-subtle: rgba(255,255,255,0.06);
  --border-light: rgba(255,255,255,0.1);
  --border-gold: rgba(212,168,67,0.3);
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --radius-xl: 28px;
  --transition-fast: 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ================= RESET & BASE ================= */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; font-size: 16px; -webkit-font-smoothing: antialiased; }
body {
  font-family: "Cairo", "Noto Sans Arabic", system-ui, -apple-system, sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
  line-height: 1.7;
  overflow-x: hidden;
}
::-webkit-scrollbar { width: 8px; }
::-webkit-scrollbar-track { background: var(--bg-primary); }
::-webkit-scrollbar-thumb { background: var(--accent-gold-dark); border-radius: 99px; }
::-webkit-scrollbar-thumb:hover { background: var(--accent-gold); }
a { color: inherit; text-decoration: none; transition: color var(--transition-fast); }
img { max-width: 100%; height: auto; display: block; }
.container { width: min(1200px, 92%); margin-inline: auto; position: relative; }

/* ================= PROGRESS BAR ================= */
.progress-bar {
  position: fixed; top: 0; left: 0; height: 3px;
  background: var(--gradient-gold); z-index: 9999; width: 0%;
  transition: width 0.1s linear;
  box-shadow: 0 0 10px rgba(212,168,67,0.5);
}

/* ================= TOPBAR ================= */
.topbar {
  border-bottom: 1px solid var(--border-subtle);
  background: var(--bg-glass);
  backdrop-filter: blur(12px);
  position: relative; z-index: 50;
}
.topbar__inner {
  display: flex; align-items: center; justify-content: space-between;
  padding: 10px 0; gap: 16px;
}
.topbar__item {
  display: flex; align-items: center; gap: 10px;
  color: var(--text-muted); font-size: 13px; font-weight: 500;
}
.dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--accent-gold);
  box-shadow: 0 0 0 4px rgba(212,168,67,0.2);
  animation: dotPulse 2.5s ease-in-out infinite;
}
@keyframes dotPulse {
  0%, 100% { transform: scale(1); box-shadow: 0 0 0 4px rgba(212,168,67,0.2); }
  50% { transform: scale(1.2); box-shadow: 0 0 0 8px rgba(212,168,67,0.1); }
}
.topbar__links { display: flex; gap: 20px; }
.topbar__links a {
  color: var(--text-muted); font-size: 13px; font-weight: 500;
  position: relative; padding: 4px 0;
}
.topbar__links a::after {
  content: ""; position: absolute; bottom: 0; right: 0;
  width: 0; height: 2px; background: var(--gradient-gold); border-radius: 99px;
  transition: width var(--transition-base);
}
.topbar__links a:hover { color: var(--text-primary); }
.topbar__links a:hover::after { width: 100%; }

/* ================= HEADER ================= */
.header {
  position: sticky; top: 0; z-index: 100;
  background: var(--bg-glass);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid var(--border-subtle);
  transition: all var(--transition-base);
}
.header--scrolled {
  box-shadow: var(--shadow-md);
  background: rgba(7, 10, 15, 0.95);
}
.header__inner {
  display: flex; align-items: center; justify-content: space-between;
  padding: 14px 0; gap: 16px;
}
.brand {
  display: flex; align-items: center; gap: 14px;
  transition: transform var(--transition-fast);
}
.brand:hover { transform: scale(1.02); }
.brand__logo-img { width: 48px; height: 48px; border-radius: var(--radius-md); }
.brand__name { font-weight: 800; font-size: 18px; letter-spacing: 0.5px; }
.brand__tag { font-size: 11px; color: var(--text-muted); margin-top: 2px; font-weight: 500; letter-spacing: 1px; text-transform: uppercase; }

/* ================= NAVIGATION ================= */
.nav__toggle {
  display: none; background: transparent; border: none;
  color: var(--text-primary); padding: 12px;
  border-radius: var(--radius-md); cursor: pointer;
  width: 48px; height: 48px; position: relative; z-index: 101;
  flex-direction: column; align-items: center; justify-content: center; gap: 6px;
}
.nav__toggle:hover { background: rgba(212,168,67,0.1); }
.nav__toggle .hamburger-line {
  display: block; width: 24px; height: 2.5px;
  background: var(--text-primary); border-radius: 99px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  transform-origin: center;
}
.nav__toggle.is-open .hamburger-line:nth-child(1) { transform: translateY(8.5px) rotate(45deg); }
.nav__toggle.is-open .hamburger-line:nth-child(2) { opacity: 0; transform: scaleX(0); }
.nav__toggle.is-open .hamburger-line:nth-child(3) { transform: translateY(-8.5px) rotate(-45deg); }
.nav__toggle.is-open { background: rgba(212,168,67,0.15); }

.nav__menu {
  list-style: none; display: flex; align-items: center; gap: 8px;
  margin: 0; padding: 0;
}
.nav__menu li { position: relative; }
.nav__menu a {
  color: var(--text-secondary); font-size: 15px; font-weight: 600;
  padding: 8px 16px; border-radius: var(--radius-sm);
  transition: all var(--transition-fast); display: block; position: relative;
}
.nav__menu a::before {
  content: ""; position: absolute; inset: 0; border-radius: inherit;
  background: linear-gradient(135deg, rgba(212,168,67,0.1), transparent);
  opacity: 0; transition: opacity var(--transition-fast);
}
.nav__menu a:hover { color: var(--text-primary); }
.nav__menu a:hover::before { opacity: 1; }
.nav__menu a.active {
  color: var(--accent-gold); background: rgba(212,168,67,0.1);
}

/* ================= BUTTONS ================= */
.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 8px;
  padding: 14px 24px; border-radius: var(--radius-md);
  background: var(--gradient-gold); color: var(--bg-primary);
  font-weight: 700; font-size: 15px; border: none; cursor: pointer;
  transition: all var(--transition-base); position: relative; overflow: hidden;
  box-shadow: var(--shadow-gold); letter-spacing: 0.3px;
}
.btn::before {
  content: ""; position: absolute; inset: 0;
  background: linear-gradient(135deg, rgba(255,255,255,0.25), transparent);
  opacity: 0; transition: opacity var(--transition-fast);
}
.btn::after {
  content: ""; position: absolute; inset: -2px; border-radius: inherit;
  background: var(--gradient-gold); opacity: 0; filter: blur(8px); z-index: -1;
  transition: opacity var(--transition-base);
}
.btn:hover { transform: translateY(-3px); box-shadow: var(--shadow-gold-lg); }
.btn:hover::before { opacity: 1; }
.btn:hover::after { opacity: 0.6; }
.btn:active { transform: translateY(-1px); }

.btn--small { padding: 10px 16px; border-radius: var(--radius-sm); font-size: 14px; }
.btn--large { padding: 16px 32px; font-size: 16px; }
.btn--outline {
  background: transparent; color: var(--accent-gold);
  border: 2px solid var(--accent-gold); box-shadow: none;
}
.btn--outline:hover { background: var(--accent-gold); color: var(--bg-primary); }

/* ================= PRODUCTS HERO ================= */
.products-hero {
  position: relative; padding: 120px 0 60px; overflow: hidden;
  background: var(--bg-secondary); border-bottom: 1px solid var(--border-subtle);
}
.products-hero::before {
  content: ""; position: absolute; inset: 0;
  background:
    radial-gradient(ellipse 80% 50% at 50% -20%, rgba(212,168,67,0.12), transparent),
    radial-gradient(ellipse 60% 40% at 80% 50%, rgba(148,163,184,0.08), transparent);
  pointer-events: none;
}
.products-hero__content {
  text-align: center; position: relative; z-index: 1;
}
.products-hero__content h1 {
  font-size: clamp(32px, 5vw, 48px); font-weight: 800;
  margin: 0 0 16px; letter-spacing: -0.5px;
}
.accent { color: var(--accent-gold); }
.products-hero__content p {
  color: var(--text-secondary); font-size: 18px;
  margin: 0 0 24px; max-width: 500px; margin-inline: auto;
}
.products-hero__breadcrumb {
  display: flex; align-items: center; justify-content: center;
  gap: 12px; color: var(--text-muted); font-size: 14px; font-weight: 500;
}
.products-hero__breadcrumb a { color: var(--accent-gold); }
.products-hero__breadcrumb a:hover { color: var(--accent-gold-light); }
.products-hero__breadcrumb span:last-child { color: var(--text-secondary); }

/* ================= SECTIONS ================= */
.section { padding: 80px 0; position: relative; }
.section--alt {
  background: var(--bg-secondary);
  border-top: 1px solid var(--border-subtle);
  border-bottom: 1px solid var(--border-subtle);
}
.section__head {
  text-align: center; margin-bottom: 48px;
  max-width: 600px; margin-inline: auto;
}
.section__head h2 {
  margin: 0 0 12px; font-size: clamp(28px, 4vw, 40px);
  font-weight: 800; letter-spacing: -0.5px;
  position: relative; display: inline-block;
}
.section__head h2::after {
  content: ""; position: absolute; bottom: -8px; left: 50%;
  transform: translateX(-50%); width: 60px; height: 3px;
  background: var(--gradient-gold); border-radius: 99px;
}
.section__head p { margin: 16px 0 0; color: var(--text-secondary); font-size: 17px; }

/* ================= GRID & CARDS ================= */
.grid { display: grid; gap: 24px; align-items: stretch; }
.cards { grid-template-columns: repeat(3, 1fr); }

.card {
  background: var(--bg-card); border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg); padding: 24px;
  box-shadow: var(--shadow-sm); position: relative; overflow: hidden;
  transition: all var(--transition-base); cursor: pointer;
  display: flex; flex-direction: column; height: 100%;
}
.card::before {
  content: ""; position: absolute; top: 0; left: 0; right: 0;
  height: 3px; background: var(--gradient-gold);
  transform: scaleX(0); transition: transform var(--transition-base);
}
.card:hover {
  transform: translateY(-8px); border-color: var(--border-gold);
  box-shadow: var(--shadow-md), var(--glow-gold);
}
.card:hover::before { transform: scaleX(1); }
.card > * { position: relative; z-index: 1; }
.card h3 { margin: 0 0 10px; font-size: 20px; font-weight: 700; }

.machine-card {
  background: #000000; border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md); padding: 16px;
  display: flex; flex-direction: column; flex: 1;
  transition: all var(--transition-base); position: relative; overflow: hidden;
}
.machine-card:hover { border-color: var(--border-gold); }
.machine-card h3 { margin: 0 0 10px; font-size: 20px; font-weight: 700; }
.machine-card > p {
  margin: 0 0 14px; color: var(--text-secondary); font-size: 14px;
  line-height: 1.7; display: -webkit-box; -webkit-line-clamp: 3;
  -webkit-box-orient: vertical; overflow: hidden;
}

.machine-card__img {
  width: 100%; height: 280px; display: flex;
  align-items: center; justify-content: center;
  overflow: hidden; border-radius: var(--radius-md);
  background: #000000; position: relative; margin-bottom: 12px; flex-shrink: 0;
}
.machine-card__img img {
  width: 100%; height: 100%; object-fit: contain;
  transition: transform var(--transition-base), filter var(--transition-base);
  filter: brightness(0.95) contrast(1.05); position: relative; z-index: 1; max-height: 250px;
}
.card:hover .machine-card__img img {
  transform: scale(1.05); filter: brightness(1.1) contrast(1.1);
}

.meta {
  margin: 12px 0 0; padding: 0 18px 0 0;
  color: var(--text-muted); font-size: 13px; flex-shrink: 0;
}
.meta li { margin: 6px 0; position: relative; }
.meta li::before {
  content: "•"; position: absolute; right: -14px; color: var(--accent-gold);
}

.product-detail-btn { margin-top: auto; align-self: flex-start; flex-shrink: 0; }

/* ================= CTA BOX ================= */
.cta-box {
  background: linear-gradient(135deg, rgba(212,168,67,0.08), rgba(123,163,201,0.05));
  border: 1px solid var(--border-gold); border-radius: var(--radius-xl);
  padding: 48px 32px; text-align: center; position: relative; overflow: hidden;
  transition: all var(--transition-base);
}
.cta-box:hover {
  border-color: var(--accent-gold); box-shadow: var(--shadow-gold);
  transform: translateY(-4px);
}
.cta-box::before {
  content: ""; position: absolute; top: 0; right: 0;
  width: 4px; height: 100%; background: var(--gradient-gold);
}
.cta-box h2 {
  font-size: clamp(24px, 3vw, 32px); font-weight: 800;
  margin: 0 0 12px; color: var(--text-primary);
}
.cta-box p {
  color: var(--text-secondary); font-size: 16px;
  margin: 0 0 24px; max-width: 500px; margin-inline: auto;
}

/* ================= MODAL ================= */
.modal {
  display: none; position: fixed; inset: 0; z-index: 10000;
  align-items: center; justify-content: center;
}
.modal.active { display: flex; }
.modal__overlay {
  position: absolute; inset: 0;
  background: rgba(0,0,0,.8); backdrop-filter: blur(12px);
}
.modal__content {
  position: relative; background: var(--bg-glass);
  border: 1px solid var(--border-subtle); border-radius: var(--radius-lg);
  padding: 32px; max-width: 600px; width: 90%; max-height: 80vh;
  overflow-y: auto; z-index: 1;
  animation: modalIn 0.3s ease; backdrop-filter: blur(16px);
}
@keyframes modalIn {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}
.modal__header {
  display: flex; justify-content: space-between; align-items: center;
  margin-bottom: 20px; padding-bottom: 16px;
  border-bottom: 1px solid var(--border-subtle);
}
.modal__header h3 { margin: 0; color: var(--accent-gold); font-size: 22px; font-weight: 700; }
.modal__close {
  background: none; border: none; color: var(--text-muted);
  font-size: 24px; cursor: pointer; transition: color var(--transition-fast);
  width: 40px; height: 40px; display: grid; place-items: center;
  border-radius: var(--radius-sm);
}
.modal__close:hover { color: var(--text-primary); background: var(--bg-tertiary); }
.modal__body { color: var(--text-secondary); line-height: 1.8; }
.modal__body h4 { color: var(--text-primary); margin: 16px 0 8px; font-size: 16px; }
.modal__body ul { padding-right: 20px; margin: 8px 0; }
.modal__body li { margin: 4px 0; }
.modal__footer { margin-top: 24px; padding-top: 16px; border-top: 1px solid var(--border-subtle); }

/* ================= WHATSAPP FLOAT ================= */
.whatsapp-float {
  position: fixed; bottom: 90px; right: 24px;
  width: 60px; height: 60px; border-radius: 50%;
  background: #25d366; color: white;
  display: flex; align-items: center; justify-content: center;
  z-index: 1000; box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4);
  transition: all var(--transition-base);
  animation: whatsappPulse 2.5s ease-in-out infinite;
}
.whatsapp-float:hover {
  transform: scale(1.15); box-shadow: 0 6px 30px rgba(37, 211, 102, 0.6);
  background: #128c7e;
}
@keyframes whatsappPulse {
  0%, 100% { box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4); }
  50% { box-shadow: 0 4px 35px rgba(37, 211, 102, 0.7); }
}

/* ================= SCROLL TOP ================= */
.scroll-top {
  position: fixed; bottom: 24px; right: 24px;
  width: 50px; height: 50px; border-radius: var(--radius-md);
  border: 1px solid var(--border-light); background: var(--bg-glass);
  backdrop-filter: blur(12px); color: var(--text-primary);
  cursor: pointer; display: flex; align-items: center; justify-content: center;
  opacity: 0; transform: translateY(20px) scale(0.9);
  pointer-events: none; transition: all var(--transition-base);
  box-shadow: var(--shadow-md); z-index: 1000;
}
.scroll-top:hover {
  transform: translateY(-2px) scale(1.05); background: var(--accent-gold);
  color: var(--bg-primary); border-color: var(--accent-gold);
  box-shadow: var(--shadow-gold-lg);
}
.scroll-top.show {
  opacity: 1; transform: translateY(0) scale(1); pointer-events: auto;
}

/* ================= FOOTER ================= */
.footer {
  border-top: 1px solid var(--border-subtle); padding: 32px 0;
  color: var(--text-muted); background: var(--bg-secondary);
}
.footer__inner {
  display: flex; align-items: center; justify-content: space-between;
  gap: 16px; flex-wrap: wrap;
}
.footer .brand__name { font-size: 20px; font-weight: 800; color: var(--text-primary); margin-bottom: 4px; }
.footer__links { display: flex; gap: 24px; }
.footer__links a {
  color: var(--text-muted); font-size: 14px; font-weight: 500;
  transition: all var(--transition-fast); position: relative;
}
.footer__links a::after {
  content: ""; position: absolute; bottom: -4px; right: 0;
  width: 0; height: 2px; background: var(--accent-gold); border-radius: 99px;
  transition: width var(--transition-base);
}
.footer__links a:hover { color: var(--text-primary); }
.footer__links a:hover::after { width: 100%; }

/* ================= SCROLL REVEAL ================= */
.reveal {
  opacity: 0; transform: translateY(40px);
  transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1),
              transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}
.reveal.active { opacity: 1; transform: translateY(0); }

/* ================= RESPONSIVE ================= */
@media (max-width: 1024px) {
  .cards { grid-template-columns: repeat(2, 1fr); }
  .theme-toggle { top: auto; bottom: 24px; left: 24px; }
}

@media (max-width: 768px) {
  .products-hero { padding: 80px 0 40px; }
  .section { padding: 60px 0; }
  .footer__inner { flex-direction: column; text-align: center; }
  .whatsapp-float { width: 52px; height: 52px; right: 16px; bottom: 80px; }
  .scroll-top { right: 16px; bottom: 16px; }
}

@media (max-width: 640px) {
  .cards { grid-template-columns: 1fr; }
  .nav__toggle { display: inline-flex; }
  .nav__menu {
    display: none; position: fixed; top: 80px; left: 4%; right: 4%;
    background: var(--bg-glass); backdrop-filter: blur(20px);
    border: 1px solid var(--border-subtle); border-radius: var(--radius-lg);
    padding: 16px; flex-direction: column; align-items: stretch; gap: 4px;
    box-shadow: var(--shadow-lg); z-index: 99;
  }
  .nav__menu.is-open { display: flex; animation: slideInMenu 0.35s cubic-bezier(0.4, 0, 0.2, 1) forwards; }
  @keyframes slideInMenu {
    from { opacity: 0; transform: translateY(-10px) scale(0.98); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }
  .nav__menu a { padding: 14px 18px; border-radius: var(--radius-md); font-size: 16px; }
  .nav__menu .btn { margin-top: 8px; text-align: center; justify-content: center; }
  .header__inner { position: relative; }
  .cta-box { padding: 32px 20px; }
}

/* ================= REDUCED MOTION ================= */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
  .reveal { opacity: 1; transform: none; }
}